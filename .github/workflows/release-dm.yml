name: Release Unseen Servant Launcher

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build dm-launcher
        run: cd apps/dm-launcher && node build.mjs

      - name: Extract version from tag
        id: version
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: "Unseen Servant Launcher v${{ steps.version.outputs.version }}"
          body: |
            ## Unseen Servant — Standalone Launcher

            Download `unseen-servant.mjs` and run:
            ```bash
            node unseen-servant.mjs
            ```

            ### Requirements
            - Node.js 18+
            - [Claude Code CLI](https://docs.anthropic.com/en/docs/claude-code/overview) (`npm i -g @anthropic-ai/claude-code`)

            ### Usage
            ```bash
            # Interactive mode
            node unseen-servant.mjs

            # With arguments
            node unseen-servant.mjs --room ABC123 --model sonnet
            ```
          files: apps/dm-launcher/dist/unseen-servant.mjs
