{
  "name": "@aidnd/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "typescript": "^5.9",
    "wrangler": "^4"
  },
  "dependencies": {
    "@aidnd/shared": "workspace:*"
  }
}
